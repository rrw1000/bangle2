class Face {
    constructor(config) {
        this.config = config;
    }

    draw(inputDate) {
        // Override this to draw your face once a second
    }

    drawStatus() {
        // Override this to draw your status line
    }
}

